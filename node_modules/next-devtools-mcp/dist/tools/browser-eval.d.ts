import { z } from "zod";
export declare const inputSchema: {
    task: z.ZodOptional<z.ZodString>;
};
type BrowserEvalArgs = {
    task?: string;
};
export declare const metadata: {
    name: string;
    description: string;
};
export declare function handler({ task }: BrowserEvalArgs): Promise<string>;
export {};
